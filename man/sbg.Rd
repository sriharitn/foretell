% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmf_sbg.R
\name{sbg}
\alias{sbg}
\title{Shifted Beta-geometric (sbg) Distribution Family Function}
\usage{
dsbg(x, shape1, shape2, log = FALSE)
psbg(x, shape1, shape2, lower.tail = TRUE, log.p = FALSE)
qsbg(p, shape1, shape2)
rsbg(n, shape1, shape2)
}
\arguments{
\item{x}{Vector of non-negative integers for `dsbg` and `psbg`.}

\item{p}{Vector of probabilities (0 <= p <= 1) for `qsbg`.}

\item{n}{Number of random variates to generate for `rsbg`.}

\item{shape1}{First shape parameter (must be > 0).}

\item{shape2}{Second shape parameter (must be > 0).}

\item{log}{Logical; if TRUE, probabilities are returned on the log scale (for `dsbg` and `psbg`).}

\item{lower.tail}{Logical; if TRUE (default), probabilities are P(X <= x), otherwise P(X > x) (for `psbg`).}

\item{log.p}{Logical; if TRUE, probabilities are returned on the log scale (for `psbg` and `qsbg`).}
}
\value{
- `dsbg`: A numeric vector of PMF values.
- `psbg`: A numeric vector of CDF values.
- `qsbg`: A numeric vector of quantile values.
- `rsbg`: A numeric vector of random variates.
}
\description{
Provides functions for the probability mass function (PMF), cumulative distribution function (CDF), quantile function, and random variate generation for the SBG distribution.
}
\examples{
# PMF example
dsbg(1:5, shape1 = 2, shape2 = 3)

# CDF example
psbg(1:5, shape1 = 2, shape2 = 3)

# Quantile example
qsbg(c(0.1, 0.5, 0.9), shape1 = 2, shape2 = 3)

# Random variates
rsbg(10, shape1 = 2, shape2 = 3)

}
